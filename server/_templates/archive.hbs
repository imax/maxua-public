<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{pageTitle}}</title>
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
      line-height: 1.6;
    }

    ul a {
      text-decoration: underline;
    }
    
    h1 {
      font-size: 2rem;
      margin-bottom: 2rem;
    }
    
    h2 {
      font-size: 1.2rem;
      margin: 2rem 0 0.5rem 0;
    }
    
    .other-years {
      margin-top: 3rem;
      border-top: 1px solid #eee;
      padding-top: 2rem;
    }
  </style>
</head>
<body>
  <a href="/">← Back to blog</a>
  <h1>Archive {{year}}</h1>

  {{#if postsByWeek.length}}
    {{#each postsByWeek}}
    <h2>{{weekRange}}</h2>
    <ul>
      {{#each posts}}
      <li><a href="{{permalink this}}">{{truncatedPreview}}</a></li>
      {{/each}}
    </ul>
    {{/each}}
  {{else}}
    <p>No posts found for {{year}}.</p>
  {{/if}}

  {{#if otherYears.length}}
  <div class="other-years">
    <p>Other years: 
      {{#each otherYears}}
      <a href="/archive?year={{this}}">{{this}}</a>{{#unless @last}} | {{/unless}}
      {{/each}}
    </p>
  </div>
  {{else}}
    <p>No older posts.</p>
  {{/if}}
</body>
</html>
